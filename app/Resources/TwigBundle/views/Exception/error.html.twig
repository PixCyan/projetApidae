{% extends "ApidaeBundle:commun:template.html.twig" %}
{% block content %}
<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
                <div class="section-title">
                    <h3>{{ "Erreur"|trans }} {{ status_code }} - {{ status_text }}</h3>
                </div>
            </div>
        </div>

        <div class="grid destination-grid-wrapper">
            <div>
                {{ "Un problème est survenu lors du chargement de votre page"|trans }}<br/>
                {{ "Si le problème persiste contactez nous."|trans }}
            </div>
        </div>
    </div>
</section>
{% endblock %}